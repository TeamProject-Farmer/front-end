import styled from '@emotion/styled';

export const HomeStyled = {
  Wrapper: styled.div`
    width: 100%;
    height: fit-content;
    display: flex;
    flex-direction: column;
    padding-top: 8px;
    padding-bottom: 20px;
    cursor: pointer;
  `,
};