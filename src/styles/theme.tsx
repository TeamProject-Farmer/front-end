import { Theme } from '@emotion/react';

const theme: Theme = {
  colors: {
    black: '#000000',
    white: '#ffffff',
    gray: '#BBBBBB',
    blue: '#156df0bd',
    darkblue: '#156DF0',
  },
  
};

export default theme;
