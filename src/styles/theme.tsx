import { Theme } from '@emotion/react';

const theme: Theme = {
  colors: {
    black: '#000000',
    white: '#ffffff',
    gray: '#BBBBBB',
    lightGray: '#D9D9D9',
    green: '#285430'
  },
};

export default theme;
