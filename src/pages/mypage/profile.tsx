import Row from '@components/Mypage/EditProfile';
import Layout from '@components/Mypage/Layout/layout';
import React from 'react';

// 프로필 수정하기 데이터는 추후 디자인파트에서 수정 가능성 있습니다. **
const index = () => {
  return (
    <Layout>
      <Row />
    </Layout>
  );
};

export default index;
