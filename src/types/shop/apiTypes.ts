//product.ts
export interface productListProps {
  productOption: string;
  categoryId: number;
  currentIndex: number;
}
export interface ShopBySizeProps {
  sizeOption: string;
  orderType: string;
  currentIndex: number;
}
