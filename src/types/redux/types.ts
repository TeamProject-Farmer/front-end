export interface UserState {
  email: string;
  username: string;
  zipCode: number;
  address: string;
  detailAddress: string;
  ph: string;
  nickname: string;
  point: number;
  grade: string;
  role: string;
  accessToken: string;
  refreshToken: string;
}
